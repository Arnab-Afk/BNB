# The permissionless package has an optional peer dep on ox@^0.8.x,
# but viem@2.x ships ox@0.12.x. This is a harmless type-level conflict
# (ox is used only for TypeScript typings in permissionless, not at runtime).
legacy-peer-deps=true
